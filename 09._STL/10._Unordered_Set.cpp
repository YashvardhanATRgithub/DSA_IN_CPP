#include<bits/stdc++.h>
using namespace std;

void explainUnorderedSet() {

    // everything is same as set
    // lower_bound() and upper_bound() doesn't work
    // It does not follow sorting of elements
    // Uniqueness of elements is followed.
}

int main() {
    explainUnorderedSet();
}